import { Spin } from 'antd';

const LoadingScreen = () => {
    return (
        <div className="loading-screen">
            <Spin size="default" />
        </div>
    );
};

export default LoadingScreen;
